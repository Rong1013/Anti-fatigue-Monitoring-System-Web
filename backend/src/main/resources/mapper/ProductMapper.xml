<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fatigue.monitoring.mapper.ProductMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.fatigue.monitoring.entity.Product">
        <id column="id" property="id"/>
        <result column="product_code" property="productCode"/>
        <result column="product_name" property="productName"/>
        <result column="product_type" property="productType"/>
        <result column="product_desc" property="productDesc"/>
        <result column="quantity" property="quantity"/>
        <result column="unit" property="unit"/>
        <result column="specification" property="specification"/>
        <result column="status" property="status"/>
        <result column="deleted" property="deleted"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, product_code, product_name, product_type, product_desc, quantity, unit,
        specification, status, deleted, create_by, create_time, update_by, update_time, remark
    </sql>

    <!-- 分页查询产品列表 -->
    <select id="selectProductPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM product
        <where>
            deleted = 0
            <if test="product.productCode != null and product.productCode != ''">
                AND product_code LIKE CONCAT('%', #{product.productCode}, '%')
            </if>
            <if test="product.productName != null and product.productName != ''">
                AND product_name LIKE CONCAT('%', #{product.productName}, '%')
            </if>
            <if test="product.productType != null and product.productType != ''">
                AND product_type = #{product.productType}
            </if>
            <if test="product.status != null">
                AND status = #{product.status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 根据产品编号查询产品 -->
    <select id="selectByProductCode" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM product
        WHERE product_code = #{productCode}
        AND deleted = 0
    </select>

</mapper>
